# Bedrock Platform Intelligence POC - Analysis Tools Configuration
# ===================================================================
# This file provides environment variable templates for analysis tools
# Copy this file to .env and fill in your actual credentials
#
# SECURITY: Never commit .env files with actual credentials to git!

# ============================================================================
# PostgreSQL Configuration (Terminal DB - Campaign Configuration)
# ============================================================================
POSTGRES_HOST=your-postgres-host.rds.amazonaws.com
POSTGRES_PORT=5432
POSTGRES_DB=your-database-name
POSTGRES_USER=your-username
POSTGRES_PASSWORD=your-password

# Alternative: Use connection URL instead of individual parameters
# POSTGRES_URL=postgresql://user:password@host:port/database

# ============================================================================
# AWS Configuration for Redshift (Aggregation DB - Performance Metrics)
# ============================================================================
# Using AWS SSO profile (recommended)
AWS_REGION=eu-west-1
AWS_PROFILE=bedrock

# OR using AWS Access Keys (not recommended for security)
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Redshift Cluster Configuration
REDSHIFT_CLUSTER_ID=your-cluster-id
REDSHIFT_DATABASE=bedrock
REDSHIFT_USER=your-redshift-username

# ============================================================================
# BidSwitch API Configuration
# ============================================================================
# IMPORTANT: Use API User credentials (not admin user)
# Create API user at: myBidSwitch UI → Users → Add User → API Account
BIDSWITCH_USERNAME=your-bidswitch-api-username
BIDSWITCH_PASSWORD=your-bidswitch-api-password

# DSP Configuration
DSP_SEAT_ID=503

# ============================================================================
# Optional: Additional Configuration
# ============================================================================
# DEBUG_MODE=1
# LOG_LEVEL=INFO


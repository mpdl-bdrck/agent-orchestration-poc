# PNP-008: Integrate NotificationLoader into Background Monitor

**Status**: 🔴 To Do  
**Phase**: Phase 2 - Background Monitor Integration  
**Priority**: High  
**Assignee**: TBD

---

## Description

Integrate the `NotificationLoader` class into the `background_monitor()` function to enable alert loading and playback.

## Acceptance Criteria

- [ ] `background_monitor()` imports `NotificationLoader`
- [ ] Creates `NotificationLoader` instance (or uses singleton)
- [ ] Calls appropriate methods based on mode:
  - [ ] Streaming: `get_next_alert()` in loop
  - [ ] Static: `get_all_alerts()` once
  - [ ] Replay: Uses `delay_seconds` from alerts
- [ ] Handles `NotificationLoader` errors gracefully
- [ ] Supports mode switching via reload

## Related Files

- `src/interface/chainlit/handlers.py`

## Dependencies

- PNP-007 (background_monitor function)
- PNP-003 (NotificationLoader)

## Notes

This ticket focuses on the integration between background monitor and loader.


# PNP-002: Hide __NOTIFY__ Transport Messages with CSS

**Status**: 🔴 To Do  
**Phase**: Phase 1 - Frontend Assets (The Overlay)  
**Priority**: High  
**Assignee**: TBD

---

## Description

Ensure CSS rules properly hide messages with `author="__NOTIFY__"` so they don't appear in the chat stream.

## Acceptance Criteria

- [ ] CSS hides messages where author contains "__NOTIFY__"
- [ ] Uses multiple selector strategies for robustness:
  - [ ] `.message-row:has(.user-name:contains("__NOTIFY__"))`
  - [ ] `.message-row:has([data-author="__NOTIFY__"])`
  - [ ] Fallback: Hide rows containing JSON signature
- [ ] Messages are completely hidden (display: none)
- [ ] Hidden messages don't affect chat layout
- [ ] Test with actual Chainlit DOM structure

## Related Files

- `public/notifications.css` (update)

## Dependencies

- PNP-001 (CSS file exists)

## Notes

Critical for DOM Bridge architecture - transport messages must be invisible to users. May need to inspect Chainlit's actual DOM structure to find correct selectors.

# PNP-005: Implement Panel Injection and Card Rendering

**Status**: 🔴 To Do  
**Phase**: Phase 1 - Frontend Assets (The Overlay)  
**Priority**: High  
**Assignee**: TBD

---

## Description

Implement the card rendering function that creates notification cards in the sidebar overlay.

## Acceptance Criteria

- [ ] `renderCard(data)` function exists
- [ ] Creates card element with correct classes
- [ ] Card includes:
  - [ ] Agent icon (emoji)
  - [ ] Agent name
  - [ ] Alert message
  - [ ] "Click to Investigate →" hint
- [ ] Card has severity class applied
- [ ] Card is appended to panel container
- [ ] Card has click handler attached
- [ ] Click handler calls `window.triggerAgent()`
- [ ] Card animates removal on click (translateX, then remove)
- [ ] Handles missing panel gracefully

## Related Files

- `public/notifications.js` (update)

## Dependencies

- PNP-004 (DOM observer exists)
- PNP-006 (Click handler exists)

## Notes

Card rendering is the visual output of the DOM Bridge. See implementation plan for card HTML structure.

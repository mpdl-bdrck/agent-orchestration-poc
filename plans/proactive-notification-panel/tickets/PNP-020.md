# PNP-020: Update .chainlit/config.toml and Start Background Monitor

**Status**: 🔴 To Do  
**Phase**: Phase 5 - Integration & Configuration  
**Priority**: High  
**Assignee**: TBD

---

## Description

Register CSS/JS files in Chainlit config and integrate background monitor into chat startup.

## Acceptance Criteria

- [ ] `.chainlit/config.toml` updated with:
  - [ ] `custom_css = "/public/notifications.css"`
  - [ ] `custom_js = "/public/notifications.js"`
- [ ] Background monitor started in `@cl.on_chat_start` function
- [ ] Check `NOTIFICATION_PANEL_ENABLED` feature flag before starting
- [ ] Use `asyncio.create_task(background_monitor())` to start
- [ ] Monitor starts without blocking chat initialization
- [ ] Log monitor startup events
- [ ] Monitor continues running throughout chat session
- [ ] CSS and JS files load correctly on page load

## Related Files

- `.chainlit/config.toml` (update)
- `src/interface/chainlit/handlers.py` (update `start()` function)

## Dependencies

- PNP-001 (CSS file exists)
- PNP-004 (JS file exists)
- PNP-007 (Background monitor function)
- PNP-011 (Error handling)

## Notes

Final integration step - connects all pieces together. See implementation plan section "5. Chainlit Configuration" for config details.

# PNP-021: Update start() Function in handlers.py

**Status**: 🔴 To Do  
**Phase**: Phase 5 - Panel Initialization  
**Priority**: High  
**Assignee**: TBD

---

## Description

Update the `start()` function to initialize the notification panel and start the background monitor.

## Acceptance Criteria

- [ ] Creates `NotificationPanel` CustomElement on chat start
- [ ] Panel initialized with empty notifications array
- [ ] Panel reference stored in `cl.user_session.set("notification_panel", panel)`
- [ ] Background monitor task started: `asyncio.create_task(background_monitor())`
- [ ] Initialization logged for debugging
- [ ] Error handling if panel creation fails

## Related Files

- `src/interface/chainlit/handlers.py` (update `start()` function)

## Dependencies

- PNP-007 (background_monitor function)
- PNP-012 (React component)

## Notes

See implementation plan section "6. Panel Initialization" for code structure.


# PNP-017: Create @cl.action_callback("dismiss_alert") Handler

**Status**: 🔴 To Do  
**Phase**: Phase 4 - Action Callbacks  
**Priority**: Medium  
**Assignee**: TBD

---

## Description

Create action callback handler for "Dismiss" button that removes alert from view.

## Acceptance Criteria

- [ ] Function `on_dismiss_alert(action: cl.Action)` exists in `handlers.py`
- [ ] Decorated with `@cl.action_callback("dismiss_alert")`
- [ ] Extracts alert_id from payload
- [ ] Sends dismissal confirmation message
- [ ] Removes action buttons (`await action.remove()`)
- [ ] Logs dismissal events

## Related Files

- `src/interface/chainlit/handlers.py` (add new function)

## Dependencies

- PNP-013 (Payload attachment)

## Notes

Simple handler that acknowledges dismissal and cleans up UI. See implementation plan section "3. Action Callbacks" for details.
